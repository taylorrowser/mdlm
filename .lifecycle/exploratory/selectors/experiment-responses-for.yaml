kind: selector-definition
id: experiment-responses-for
version: 1
description: experiment-responses-for
parameters:
  - name: feedback
    kind: revision
    types:
      - FDB
result_kind: revision
query:
  from:
    relation: incoming-links
    of: feedback
    link: responds-to
    emit: source
    types:
      - EXP
  as: item
  distinct: true
  order_by:
    - identity.revision_id
