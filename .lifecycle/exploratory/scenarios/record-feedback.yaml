kind: scenario-definition
id: record-feedback
version: 1
description: record-feedback
phases:
  - phase-0-exploration
inputs:
  - name: observation
    types:
      - OBS
    cardinality: one
    identity: revision
outputs:
  - name: feedback
    types:
      - FDB
    cardinality: one
    required_links:
      - link: responds-to
        target:
          input: observation
    required_payload:
      publication: recorded
prompt_ref: prompts/record-feedback.md@1
review_policy_ref: review-applicability@1
completion: execution.integrity.contract_valid == true
resolves:
  - stakeholder-feedback-required
prohibited_inputs: []
batching: single
participation:
  policy_ref: stakeholder-feedback@1
  arguments:
    subject: observation
authority_evidence:
  output: feedback
  type: FDB
