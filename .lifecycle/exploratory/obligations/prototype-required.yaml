kind: obligation-definition
id: prototype-required
version: 1
description: prototype-required
phases:
  - phase-0-exploration
for_each: select("current-experiments@1", {})
subject_as: subject
satisfied_when: 'exists("trials-for@1", {experiment: subject})'
status_rules:
  - status: ready
    priority: 100
    when: "true"
    reason: prototype-required
default_status: blocked
resolve_with:
  scenario: prepare-prototype@1
  inputs:
    experiment: subject
waiver_policy_ref: waiver-applicability@1
