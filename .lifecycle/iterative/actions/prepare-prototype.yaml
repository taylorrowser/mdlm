kind: action-definition
id: prepare-prototype
version: 1
description: prepare-prototype
capability: prototype
prompt_ref: prompts/prepare-prototype.md@3
types:
- TRY
subjects: select("current-experiments@1", {})
inputs:
  experiment: subject
when: '!(exists("trials-for@2", {experiment: subject})) && (true)'
priority: 100
links:
  TRY:
    explores: experiment
fixed_payload:
  TRY:
    publication: recorded
