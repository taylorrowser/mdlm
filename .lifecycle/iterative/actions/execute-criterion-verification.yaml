kind: action-definition
id: execute-criterion-verification
version: 1
description: execute criterion verification
capability: independent-result
prompt_ref: prompts/execute-verification.md@2
types:
- RES
subjects: select("current-trials@2", {})
inputs:
  product: subject
  experiment: 'one("experiment-for@1", {trial: subject})'
  activities: 'select("selected-verification-activities@1", {product: subject})'
when: 'true'
priority: 90
optional: true
links:
  RES:
    executes: product
    verifies: experiment
fixed_payload:
  RES:
    publication: recorded
    title: Verification result
