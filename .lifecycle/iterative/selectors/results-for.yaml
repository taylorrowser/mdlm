kind: selector-definition
id: results-for
version: 1
description: results for
parameters:
- name: implementation
  kind: revision
  types:
  - IMP
  - TRY
result_kind: revision
query:
  from:
    relation: incoming-links
    of: implementation
    link: executes
    emit: source
    types:
    - RES
  as: item
  where: 'state(item, "validity") == "valid" && none("superseding-results@1", {result: item})'
  distinct: true
  order_by:
  - identity.revision_id
