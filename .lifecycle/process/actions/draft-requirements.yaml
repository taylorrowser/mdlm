kind: action-definition
id: draft-requirements
version: 1
description: Write one small requirement set
capability: requirements
prompt_ref: prompts/draft-requirements.md@5
types:
- REQ
- DCP
- RQS
inputs: {}
when: '!(exists("current-requirements@1", {})) && (true)'
priority: 100
fixed_payload:
  REQ:
    publication: recorded
  DCP:
    publication: recorded
  RQS:
    publication: recorded
