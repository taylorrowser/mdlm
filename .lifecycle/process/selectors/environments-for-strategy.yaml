kind: selector-definition
id: environments-for-strategy
version: 1
description: Current valid environments realizing one exact verification strategy Revision.
parameters:
  - {name: strategy, kind: revision, types: [VSP]}
result_kind: revision
query:
  from: {relation: incoming-links, of: strategy, link: realizes, emit: source, types: [ENV]}
  as: environment
  where: >-
    state(environment, "disposition") == "active"
    && state(environment, "validity") == "valid"
    && none("newer-revisions-for@1", {subject: environment})
  distinct: true
  order_by: [identity.id, identity.revision]
