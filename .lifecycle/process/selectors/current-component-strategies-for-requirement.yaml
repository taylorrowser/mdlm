kind: selector-definition
id: current-component-strategies-for-requirement
version: 1
description: Current component VSP Revisions covering one exact accepted SYS Revision.
parameters:
  - {name: requirement, kind: revision, types: [SYS]}
result_kind: revision
query:
  from: {relation: incoming-links, of: requirement, link: governs-revision, emit: source, types: [VSP]}
  as: strategy
  where: >-
    strategy.payload.level == "component"
    && state(strategy, "disposition") == "active"
    && state(strategy, "validity") == "valid"
    && none("newer-revisions-for@1", {subject: strategy})
  distinct: true
  order_by: [identity.id, identity.revision]
