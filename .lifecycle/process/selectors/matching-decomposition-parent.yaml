kind: selector-definition
id: matching-decomposition-parent
version: 1
description: The supplied stakeholder parent when one DWP plan canonically covers its exact Revision.
parameters:
  - {name: plan, kind: revision, types: [DWP]}
  - {name: parent, kind: revision, types: [STK]}
result_kind: revision
query:
  from:
    selector: decomposition-parents-for@1
    arguments: {plan: plan}
  as: linked
  where: 'linked.identity.id == parent.identity.id'
  distinct: true
  order_by: [identity.revision_id]
