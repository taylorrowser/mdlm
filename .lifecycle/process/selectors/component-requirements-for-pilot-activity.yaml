kind: selector-definition
id: component-requirements-for-pilot-activity
version: 1
description: Exact component requirement Revision exercised by one pilot activity.
parameters:
  - {name: activity, kind: revision, types: [VER]}
result_kind: revision
query:
  from: {relation: outgoing-links, of: activity, link: verifies-revision, emit: target, types: [CMP]}
  as: requirement
  distinct: true
  order_by: [identity.revision_id]
