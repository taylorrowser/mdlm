kind: selector-definition
id: stakeholder-decomposition-parents-for
version: 1
description: Exact STK parents for a SYS-target decomposition, as a typed adapter over the shared parent relation.
parameters:
  - {name: plan, kind: revision, types: [DWP]}
result_kind: revision
query:
  from: {relation: outgoing-links, of: plan, link: decomposes, emit: target, types: [STK]}
  as: parent
  where: 'plan.payload.target_child_type == "SYS"'
  distinct: true
  order_by: [identity.revision_id]
