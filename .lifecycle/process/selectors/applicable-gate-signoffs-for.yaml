kind: selector-definition
id: applicable-gate-signoffs-for
version: 1
description: Valid gate-signoff decisions whose required independent review has passed.
parameters:
  - {name: candidate, kind: baseline, types: [BSL]}
result_kind: revision
query:
  from:
    selector: gate-signoff-decisions-for@1
    arguments: {candidate: {var: candidate}}
  as: decision
  where:
    exists:
      selector: passing-reviews-for@1
      arguments: {subject: {var: decision}}
  distinct: true
  order_by: [identity.revision_id]
