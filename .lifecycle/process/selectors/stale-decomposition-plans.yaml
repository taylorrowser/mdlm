kind: selector-definition
id: stale-decomposition-plans
version: 1
description: Latest planning DWP Revisions whose exact Review support is no longer wholly current.
parameters: []
result_kind: revision
query:
  from: {collection: revisions, types: [DWP]}
  as: plan
  where: >-
    plan.payload.stage == "planning"
    && state(plan, "disposition") == "active"
    && state(plan, "validity") == "valid"
    && none("newer-revisions-for@1", {subject: plan})
    && !exists("planning-dwp-review-support-is-current@1", {plan: plan})
  distinct: true
  order_by: [identity.id, identity.revision]
