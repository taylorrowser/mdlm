kind: obligation-definition
id: expectation-correction
version: 1
description: expectation correction
phases:
- phase-0-tiny-product
for_each: select("expectation-failures@1", {})
subject_as: subject
satisfied_when: 'false'
status_rules:
- status: ready
  priority: 100
  when: 'true'
  reason: expectation correction
default_status: blocked
resolve_with:
  scenario: correct-expectations@1
  inputs:
    failure: subject
    subject: 'one("requirements-for-result@1", {result: subject})'
waiver_policy_ref: waiver-applicability@1
