kind: obligation-definition
id: requirements-review-required
version: 1
description: requirements review required
phases:
- phase-0-tiny-product
for_each: select("current-requirements@1", {})
subject_as: subject
satisfied_when: 'exists("reviews-for@1", {subject: subject})'
status_rules:
- status: ready
  priority: 100
  when: 'true'
  reason: requirements review required
default_status: blocked
resolve_with:
  scenario: review-requirements@1
  inputs:
    subject: subject
waiver_policy_ref: waiver-applicability@1
