kind: obligation-definition
id: verification-required
version: 1
description: verification required
phases:
- phase-0-tiny-product
for_each: select("current-implementations@1", {})
subject_as: subject
satisfied_when: 'exists("results-for@1", {implementation: subject})'
status_rules:
- status: ready
  priority: 100
  when: 'true'
  reason: verification required
default_status: blocked
resolve_with:
  scenario: execute-verification@1
  inputs:
    implementation: subject
    requirements: 'one("requirements-for@1", {implementation: subject})'
waiver_policy_ref: waiver-applicability@1
