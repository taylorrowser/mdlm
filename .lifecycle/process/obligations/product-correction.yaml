kind: obligation-definition
id: product-correction
version: 1
description: product correction
phases:
- phase-0-tiny-product
for_each: select("implementation-failures@1", {})
subject_as: subject
satisfied_when: 'false'
status_rules:
- status: ready
  priority: 100
  when: 'true'
  reason: product correction
default_status: blocked
resolve_with:
  scenario: correct-product@2
  inputs:
    failure: subject
    subject: 'one("implementation-for-result@1", {result: subject})'
    requirements: 'one("requirements-for-result@1", {result: subject})'
waiver_policy_ref: waiver-applicability@1
