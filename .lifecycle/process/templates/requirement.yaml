kind: type-template-definition
id: requirement
version: 1
description: Singular rationale-backed requirement with an explicit verification intent.
extends: rationale-bearing@1
payload_schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  required: [statement, verification_intent]
  properties:
    statement: {type: string, minLength: 1}
    verification_intent: {type: string, minLength: 1}
outgoing_links: []
