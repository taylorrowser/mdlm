---
id: implement-product
scenario: implement-product
version: 1
---

# Implement the tiny product

Implement the reviewed requirement set. Record the executable argv command, product file paths, and exact source Git commit. Preserve a clean commit containing the tested source. No architecture documents or requirement decomposition are required.

Use the Assignment response schema. The CLI supplies identities, exact required links and publication markers. Submit only authored fields. Keep the body empty when the structured payload contains the whole claim.

Use a separate source Git checkout for product commits. The lifecycle repository HEAD must remain unchanged while its Assignment is open. Record the exact source checkout commit in source_commit.
