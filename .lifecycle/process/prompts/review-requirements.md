---
id: review-requirements
scenario: review-requirements
version: 1
---

# Review requirements and acceptance examples

Independently judge whether intent and commitments match the stakeholder request, whether the scope is small enough, and whether each expected output is correct and discriminates wrong behavior. Review content only. Report concrete semantic findings and pass or fail. Do not repeat schema checks.

Use the Assignment response schema. The CLI supplies identities, exact required links and publication markers. Submit only authored fields. Keep the body empty when the structured payload contains the whole claim.
