---
id: review-phase-2-datum
version: 1
scenario: review-phase-2-datum
skills:
  - skills/contextual-artifact-review.md@2
---

# Review one Phase 2 datum and its exact context

Review the exact `subject` independently under the resolved rubric. Read every
exact `review_context_members` Revision. Author preflight and prior judgment are
not Review evidence.

Return one `review_context` and one `review`. The kernel derives the frozen
baseline's exact subject, members, evidence partition, hashes, scope, and
provenance from this Assignment, then publishes the BSL and REV together or
publishes neither. The REV must have exactly one `reviews` link to `subject` and
one `contextualizes` link to the BSL proposed in this response.

Use `simplification-product-definition` only for an intent candidate or Phase 2
planning DWP selected for that judgment. Use `contextual` otherwise.
Apply the complete blocking-finding test from the Review skill. A failed Review
must classify its complete blocking set as `stakeholder` or `package-evidence`.
Keep collateral findings separate and add the package-required exact `flags`
links for affected SYS, ASP, ICSP, or DWP Revisions. Do not repair the subject
during Review.
