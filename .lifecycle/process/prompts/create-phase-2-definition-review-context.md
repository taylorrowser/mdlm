---
id: create-phase-2-definition-review-context
version: 1
scenario: create-phase-2-definition-review-context
---

# Create the architecture-wide definition Review Context

Read, in order:

1. `skills/lifecycle-data.md@1`
2. `skills/traceability.md@1`
3. `skills/baseline-model.md@1`

For each representative planning DWP, deterministically materialize one frozen
review-context BSL containing every and only the supplied architecture-wide ASP,
ICSP, DWP, and SYS definition members. The DWP is the context scope and is included
exactly once. Use exact Revision identities in canonical order.

This is package-owned context construction, not an authoring judgment. Do not add,
omit, search for, or substitute members. The kernel owns exact source-byte hashing,
process provenance, atomic freeze, and verification. The operator contributes no
semantic context decision.
