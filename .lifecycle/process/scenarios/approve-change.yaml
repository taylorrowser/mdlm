kind: scenario-definition
id: approve-change
version: 1
description: Approve the requested scope
phases:
- phase-0-tiny-product
inputs:
- name: subject
  types:
  - CHG
  cardinality: one
  identity: revision
outputs:
- name: review
  types:
  - REV
  cardinality: one
  required_links:
  - link: reviews
    target:
      input: subject
  required_payload:
    publication: recorded
prompt_ref: prompts/approve-change.md@1
review_policy_ref: review-applicability@1
completion: execution.integrity.contract_valid == true && (true)
resolves:
- change-approval-required
prohibited_inputs: []
batching: single
participation:
  policy_ref: stakeholder-acceptance@1
  arguments:
    subject: subject
authority_evidence:
  output: review
  type: REV
