kind: scenario-definition
id: revise-pilot-vai-after-review
version: 1
description: Correct one failed source-blind pilot procedure in the same VAI lineage while retaining every exact activity, ENV, ART, claim-class, and case binding.
phases: [phase-1-product-assurance]
inputs:
  - name: implementation
    types: [VAI]
    cardinality: one
    identity: revision
    conditions: >-
      implementation.payload.kind == "pilot"
      && implementation.payload.independence_mode == "source-blind"
      && exists("failing-reviews-for@1", {subject: implementation})
      && none("newer-revisions-for@1", {subject: implementation})
  - name: failed_reviews
    types: [REV]
    cardinality: one-or-more
    identity: revision
    conditions: >-
      exists("matching-failing-review-for@1",
        {subject: implementation, review: failed_reviews})
  - name: activity
    types: [VER]
    cardinality: one
    identity: revision
    conditions: >-
      count("verification-activities-for-implementation@1",
        {implementation: implementation}) == 1
      && activity == one("verification-activities-for-implementation@1",
        {implementation: implementation})
  - name: environment
    types: [ENV]
    cardinality: one
    identity: revision
    conditions: >-
      count("environments-for-implementation@1",
        {implementation: implementation}) == 1
      && environment == one("environments-for-implementation@1",
        {implementation: implementation})
  - name: execution_target
    types: [ART]
    cardinality: one
    identity: revision
    conditions: >-
      count("artifact-targets-for-implementation@1",
        {implementation: implementation}) == 1
      && execution_target == one("artifact-targets-for-implementation@1",
        {implementation: implementation})
outputs:
  - name: replacement
    types: [VAI]
    cardinality: one
    required_links:
      - {link: realizes, target: {input: activity}}
      - {link: uses, target: {input: environment}}
      - {link: targets, target: {input: execution_target}}
      - {link: corrects-review, target: {input: failed_reviews}}
  - name: authorization
    types: [DEC]
    cardinality: one
    required_links:
      - {link: justifies, target: {output: replacement}}
prompt_ref: prompts/revise-pilot-vai-after-review.md@1
review_policy_ref: review-applicability@1
participation:
  policy_ref: phase-1-assurance-correction-participation@1
  arguments: {subject: implementation}
authority_evidence: {output: authorization, type: DEC}
completion: >-
  execution.integrity.contract_valid == true
  && replacement.identity.id == implementation.identity.id
  && replacement.identity.type == implementation.identity.type
  && replacement.payload.execution_procedure.deadline_claim == "infrastructure-safety-only"
  && replacement.payload.execution_procedure.cleanup == "guaranteed"
  && replacement.payload.execution_procedure.aggregation == "continue-through-all-cases"
  && exists("corrected-pilot-verification-implementation-revisions-for@1",
    {implementation: implementation})
  && authorization.payload.effective_scope == replacement.identity.revision_id
  && (
    (policy("phase-1-assurance-correction-participation@1",
      {subject: implementation}).authority_mode == "autonomous"
      && authorization.payload.kind == "decision"
      && none("phase-1-assurance-correction-decisions-for@1",
        {replacement: replacement}))
    || (policy("phase-1-assurance-correction-participation@1",
      {subject: implementation}).authority_mode == "attended"
      && authorization.payload.kind == "scope"
      && count("phase-1-assurance-correction-decisions-for@1",
        {replacement: replacement}) == 1)
  )
resolves: [pilot-vai-review-correction-required]
prohibited_inputs: [mutation of failed VAI, RUN, RES, or Review history, changed activity binding, changed environment binding, changed execution target binding, changed pilot claim class, product source, product unit tests, private implementation details]
batching: single
