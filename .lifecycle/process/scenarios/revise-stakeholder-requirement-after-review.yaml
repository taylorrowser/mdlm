kind: scenario-definition
id: revise-stakeholder-requirement-after-review
version: 1
description: Replace one exact STK while preserving its product-specification parent, correction evidence, and immutable history.
phases: [phase-0-wayfinding]
inputs:
  - {name: subject, types: [STK], cardinality: one, identity: revision}
  - {name: prior_failed_reviews, types: [REV], cardinality: zero-or-more, identity: revision}
  - {name: failed_reviews, types: [REV], cardinality: zero-or-more, identity: revision}
  - {name: simplification_reviews, types: [REV], cardinality: zero-or-more, identity: revision}
  - {name: gate_rejections, types: [DEC], cardinality: zero-or-more, identity: revision}
  - {name: product_questions, types: [QST], cardinality: zero-or-more, identity: revision}
  - {name: product_answer_decisions, types: [DEC], cardinality: zero-or-more, identity: revision}
  - {name: product_specification, types: [PSP], cardinality: one, identity: revision}
outputs:
  - name: replacement
    types: [STK]
    cardinality: one
    identity_from: {input: subject}
    required_links:
      - {link: corrects-review, target: {input: prior_failed_reviews}}
      - {link: corrects-review, target: {input: failed_reviews}}
      - {link: corrects-review, target: {input: simplification_reviews}}
      - {link: corrects-gate-rejection, target: {input: gate_rejections}}
      - {link: incorporates-answer, target: {input: product_answer_decisions}}
      - {link: derived-from, target: {input: product_specification}}
prompt_ref: prompts/revise-stakeholder-requirement-after-review.md@1
review_policy_ref: review-applicability@1
completion: >-
  execution.integrity.contract_valid == true
  && replacement.identity.id == subject.identity.id
  && replacement.identity.type == subject.identity.type
  && every("incorporated-product-answer-decisions-for-foundation-subject@1",
    {subject: subject}, authority =>
      !every("incorporated-product-answer-decisions-for-foundation-subject@1",
        {subject: replacement}, replacement_authority =>
          replacement_authority != authority))
  && every("current-product-specifications-for-requirement@1",
    {requirement: subject}, product =>
      !every("current-product-specifications-for-requirement@1",
        {requirement: replacement}, replacement_product =>
          replacement_product != product))
resolves: [stakeholder-requirement-review-correction-required]
prohibited_inputs: [mutation of the failed Revision, deletion of failed Review history, replacement of product-specification parent, unrelated scope expansion]
batching: single
